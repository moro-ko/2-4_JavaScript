$(function(){
// 上から下にスライド（display:none　追加）
  // $('.box1').slideDown();

// 下から上にスライド（display:none 削除）
  // $('.box1').slideUp();

// 非表示の要素を表示（display:none　追加）
  // $('.box1').show();
  // $('.box1').css({'background-color': '#0000FF'});

// 表示されている要素を非表示にする（display:none 削除）
  $('.box1').hide();
});